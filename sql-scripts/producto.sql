CREATE TABLE `practico`.`Producto` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `creado` DATETIME NULL,
  `actualizado` DATETIME NULL,
  `nombre` VARCHAR(100) NULL,
  `Precio` INT(5) NULL,
  PRIMARY KEY (`id`));
